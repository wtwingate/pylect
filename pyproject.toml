[project]
name = "lectionary"
version = "0.1.0"
dependencies = [
    "PyMuPDF",
    "pytest",
    "python-dateutil",
    "python-dotenv",
    "requests"
]

[project.scripts]
lectionary = "lectionary.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
