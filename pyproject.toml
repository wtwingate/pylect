[project]
name = "lectionary"
version = "0.3.0"
dependencies = [
    "PyMuPDF",
    "pyperclip",
    "pytest",
    "python-dateutil",
    "python-dotenv",
    "requests"
]

[project.scripts]
lectionary = "lectionary.cli:main"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
